#include<iostream>
using namespace std;

int main(){
    int a =2,b=5;

    cout<<a+b<<endl;
    cout<<a-b<<endl;
    cout<<a*b<<endl;
    cout<<a/b<<endl;
    cout<<a%b<<endl;//Modulus operator

    

    return 0;
}

//a<b ->true when a is less than b otherwise false.
//a>b ->true when a is greater than b otherwise false.
//a==b ->true when a and b both are equal otherwise false.
//a>=b ->true when a is either greater than b or equal to b otherwise false.
//a<=b ->true when a is less than b or equal to b otherwise false.
//a!=b ->true when a and b are not equal otherwise false.

//(a<=b) && (a<b) && () ->AND operator
//(a<=b) || (a>b) ->OR Operator